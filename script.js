//parameters vast
var canvx = 650;
var canvy = 750;

//parameters changing
var gamestate = 0;

function setup() {
  createCanvas(canvx, canvy);
}

function draw() {
  background(10);
  if (gamestate = 0);
}